apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
  name: gigra
spec:
  rules:
    - host: gigra.stytex.cloud
      http:
        paths:
          - backend:
              serviceName: gigra
              servicePort: 80
            path: /
